#!/usr/bin/env bash

# Executes CURL GET without logging into container's bash
#
# Example:
# kubernetes_curl_get service-gateway/profiles/1

function kubernetes_http_get(){
    set -l HOST $(echo $1 | egrep -o '^[^/]+')
    echo curl -sS $1 | tee >(k8E $HOST bash)
}

kubernete_http_get $@
