# navigation
alias o "open"
alias :q "exit"
alias .. "cd .."
alias ... '../..'
alias .... '../../..'
alias ..... '../../../..'

# browsing
alias less "less -R"
alias tailf "tail -f"
alias ls "ls -Gh"
alias ll "ls -al"
alias lsg "ll | grep"
alias rr "ranger"

# disk
alias df "df -h"
alias du "du -h -d 2"
