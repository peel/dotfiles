language: generic
os:
  - osx

sudo: required

before-install:
  - brew update
  - brew tap Homebrew/bundle

install:
  - brew install stow
  - brew install fish

script:
  - make osx
  - make shell
  - make editor
  - make link 
