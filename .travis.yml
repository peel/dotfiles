language: generic
os:
  - osx

sudo: required

before-install:
  - brew update
  - brew tap Homebrew/bundle

install:
  - brew uninstall git 
  - brew uninstall tmux

script:
  - make nix
  - make link
  - make editor
  - make osx
  - make clean
