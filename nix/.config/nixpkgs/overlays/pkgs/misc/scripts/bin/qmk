#!/usr/bin/env bash

qmk() {
  local QMK_PATH="$1"
  rg -A 4 "= LAYOUT_ortho_4x12(" --fixed-strings --replace "" \
     $QMK_PATH | column -t -s"\t" | zenity --text-info --font=PragmataPro --width 1500 --height 1500'';
}

qmk "$@"
